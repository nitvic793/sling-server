"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Parent');
const Fixture = require('../../fixtures/ParentFixture');

describe('models:Parent', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
