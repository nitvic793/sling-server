"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/NoticeBoard');
const Fixture = require('../../fixtures/NoticeBoardFixture');

describe('models:NoticeBoard', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
