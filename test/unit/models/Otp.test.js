"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Otp');
const Fixture = require('../../fixtures/OtpFixture');

describe('models:Otp', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
