"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Student');
const Fixture = require('../../fixtures/StudentFixture');

describe('models:Student', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
