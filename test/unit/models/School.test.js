"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/School');
const Fixture = require('../../fixtures/SchoolFixture');

describe('models:School', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
