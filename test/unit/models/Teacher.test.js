"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Teacher');
const Fixture = require('../../fixtures/TeacherFixture');

describe('models:Teacher', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
