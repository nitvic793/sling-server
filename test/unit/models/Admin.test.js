"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Admin');
const Fixture = require('../../fixtures/AdminFixture');

describe('models:Admin', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
