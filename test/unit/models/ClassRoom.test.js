"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/ClassRoom');
const Fixture = require('../../fixtures/ClassRoomFixture');

describe('models:ClassRoom', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
