"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Review');
const Fixture = require('../../fixtures/ReviewFixture');

describe('models:Review', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
