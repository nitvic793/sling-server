"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ReviewController');

describe('controllers:ReviewController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
