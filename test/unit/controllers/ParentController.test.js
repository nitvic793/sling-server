"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ParentController');

describe('controllers:ParentController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
