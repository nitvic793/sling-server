"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/StudentController');

describe('controllers:StudentController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
