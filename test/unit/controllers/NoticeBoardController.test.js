"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/NoticeBoardController');

describe('controllers:NoticeBoardController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
