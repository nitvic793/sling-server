"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ClassRoomController');

describe('controllers:ClassRoomController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
