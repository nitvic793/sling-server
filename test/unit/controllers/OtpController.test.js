"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/OtpController');

describe('controllers:OtpController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
